'use strict';

let config = {
  keys: {
    'madalin': 'Madalin_Secret_Key',
    'test': 'DemoTestSecret'
  },
  keepalive: {
    timeout: 300000 // setTimeout 5 minute in ms
  }
}

module.exports = config;